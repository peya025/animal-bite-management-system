# Production mobile API. This public URL is not a secret, but do not commit a
# real .env file because it can also contain environment-specific settings.
API_BASE_URL=https://your-railway-backend-url.up.railway.app/api/mobile
CLINIC_ID=1
