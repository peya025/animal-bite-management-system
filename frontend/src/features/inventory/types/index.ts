/**
 * Inventory feature types.
 * Re-exports the shared InventoryItem, InventoryStats, and
 * InventoryTransaction types for convenient feature-local imports.
 */
export type {
  InventoryItem,
  InventoryStats,
  InventoryTransaction,
  InventoryStatus,
} from '../../../shared/types';
