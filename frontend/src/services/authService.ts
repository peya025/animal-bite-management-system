/**
 * @deprecated Import from `../shared/services/auth.service` instead.
 * Shim — keeps all existing `../../services/authService` imports working.
 */
export { default } from '../shared/services/auth.service';
