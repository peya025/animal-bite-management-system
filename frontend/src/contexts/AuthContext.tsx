/**
 * @deprecated Import from `../shared/contexts/AuthContext` instead.
 * Shim — keeps all existing `../../contexts/AuthContext` imports working.
 */
export { AuthProvider, useAuth } from '../shared/contexts/AuthContext';
