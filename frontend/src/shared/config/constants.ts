/**
 * Re-exports all application constants from the canonical source.
 * Import from here when writing new code inside `features/` or `shared/`.
 *
 * @example
 * import { ROLES, DEFAULT_PAGE_SIZE } from '../../shared/config/constants';
 */
export * from '../../constants';
